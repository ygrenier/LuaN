<UserControl x:Class="LuaN.Studio.Views.TextEditorDocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LuaN.Studio.Views"
             xmlns:Controls="clr-namespace:LuaN.Studio.Controls" 
             xmlns:dt="clr-namespace:LuaN.Studio.DesignTime"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=dt:DtTextEditorDocumentViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Controls:TextEditorControl 
            Document="{Binding TextContent}"
            TextDefinition="{Binding TextDefinition}"
            />
    </Grid>
</UserControl>
